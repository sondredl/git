BUILD_DIR = o

# build: all

# build:
all:
	cmake -B o/ -S .  -G Ninja
	cd $(BUILD_DIR) && Ninja
	# cmake -S . -B . -G Ninja
	# Ninja
	cp cleanCode/pre-commit .git/hooks/pre-commit
	chmod +x .git/hooks/pre-commit
